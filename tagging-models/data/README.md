# Input Data

This directory stores local ROOT inputs and the geometry file. The default input
files, their prompt/source positions, and the geometry path are defined in
`candidates_extraction/config.py`.
